# User:Group for volume permissions (match host user to avoid permission issues)
USER_ID=1000
GROUP_ID=1000

TZ=Etc/UTC

# Optional: increase JVM memory if you see OutOfMemoryException (e.g. 2g, 4g)
# JAVA_TOOL_OPTIONS=-Xmx4g

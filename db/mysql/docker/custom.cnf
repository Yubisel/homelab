# MySQL custom configuration
# Loaded from /etc/mysql/conf.d/99-custom.cnf

[mysqld]
max_connections = 100
character-set-server = utf8mb4
collation-server = utf8mb4_unicode_ci

[client]
default-character-set = utf8mb4

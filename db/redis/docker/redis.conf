# Redis configuration
# Data directory and persistence
dir /data
appendonly yes
appendfsync everysec

# Memory
maxmemory 256mb
maxmemory-policy allkeys-lru

# Network
bind 0.0.0.0
protected-mode yes
# requirepass is set via command line from REDIS_PASSWORD
